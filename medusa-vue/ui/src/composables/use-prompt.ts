// TODO: Port usePrompt after Prompt component is available
// It requires dynamic mounting of a Vue component which depends on the UI library being present.

export const usePrompt = () => {
    throw new Error("usePrompt is not yet implemented in Vue version.")
}
